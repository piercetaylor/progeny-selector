# Not used by `scripts/build_shinylive.py`, which writes its own requirements.txt
# (pyyaml, pandas, numpy) into the staging directory it exports (docs/adr/0009).
# This file is unused by `shiny run` too; it exists only for an export run directly
# against this directory, which does not bundle the progeny_selector package.
pyyaml
